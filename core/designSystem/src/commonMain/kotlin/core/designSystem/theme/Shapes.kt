package core.designSystem.theme

import androidx.compose.foundation.shape.RoundedCornerShape

object Shapes {
    val buttonPrimary = RoundedCornerShape(AppDimensions.size_8)
    val outlineInputField = RoundedCornerShape(AppDimensions.size_8)
    val small = RoundedCornerShape(AppDimensions.size_4)
    val medium = RoundedCornerShape(AppDimensions.size_8)
    val large = RoundedCornerShape(AppDimensions.size_12)
}
